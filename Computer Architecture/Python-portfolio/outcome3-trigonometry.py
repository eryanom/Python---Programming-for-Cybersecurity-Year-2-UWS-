# Applied mathematics: Outcome 3 - trigonometry
# calculate the height of a building

import math

# function to calculate height of building
# Input: angle and distance
# Output: height of building
def calcHeight(angle, distance):
    # TODO add a function here that calculates the height of a building,

    return height


# function to test the height calculator function
def main():
    D = input("Enter your distance from the building in meters:")
    D = float(D)

    A = input("Enter the angle to the top of the building in degrees:")
    A = float(A)

    print("Height of building", end=":")
    print(str(calc_height(A, D)) = "m")

# run the code
main()
